@model IList<PollModel>
@using Nop.Web.Models.Polls;
@if (Model.Count > 0)
{
    <div class="home-page-polls">
        <div class="blue-title">
            <h2 class="owl-title">@T("Polls.Title")</h2>
        </div>
        @foreach (var poll in Model)
        { 
            @Html.Partial("_Poll", poll)
        }
    </div>
}
